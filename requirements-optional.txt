# Optional / hardware-native packages (may require system libs)
# Windows: needs PortAudio + MSVC build tools for pyaudio
# Debian/Ubuntu: sudo apt-get install -y portaudio19-dev python3-dev
# macOS: brew install portaudio

pyaudio>=0.2.14
